export type MentorMode = 'hard' | 'empathetic';

export type DashboardOverview = {
  dayScore: number;
  mentalState: number;
  streakDays: number;
  pendingTasks: number;
  motivationalLine: string;
};
